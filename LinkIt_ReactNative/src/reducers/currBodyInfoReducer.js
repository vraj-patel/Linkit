const initialState = {
    groupCardsDisplayed: false
}

  const currBodyInfoReducer = (state = initialState, action) => {
    switch(action.type) {
      
    }
    return state
  }
  
  export default currBodyInfoReducer;