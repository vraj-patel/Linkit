const linkGroups = (state=[], action) => {
    // switch(action.type) {
    //     case 'ADD_GROUP':
    //         return [
    //             ...state, {
    //                 id: action.id,
    //                 name: action.name,
    //                 color: action.color,
    //                 links: []
    //             }
    //         ]
    //     default: 
    //         return state
    // }
    return state;
}

export default linkGroups;